<div class="container">
  <div class="row">
    <div class="col-sm-12 text-center">
      <h1>Carreras</h1>
    </div>
    <div class="col-sm-12 text-right">
      <hr>
      <%= link_to "Nuevo Carrera", new_race_path, class: "btn btn-primary" %>
      <%#= link_to "Editar Corrredore", edit_rider_path(@rider), class: "btn btn-primary" %>
      <hr>
    </div>

    <table class="table table-hover">
      <thead>
        <tr>
          <th>Title</th>
          <th>Fecha</th>
          <th>Descripción</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @races.each do |race| %>
          <tr>
            <td><%= race.title %></td>
            <td><%= race.date %></td>
            <td><%= race.description %></td>
            <td class="text-right">
              <%= link_to "<span class='glyphicon glyphicon-pencil'></span>".html_safe, edit_race_path(race), class: "btn btn-xs btn-primary" %>
              <%= link_to "<span class='glyphicon glyphicon-eye-open'></span>".html_safe, race_path(race), class: "btn btn-xs btn-info" %>  
            </td>
          </tr>
        <%end%>
      </tbody>
    </table>
  </div>
</div>